[project]
name = "agentic-langgraph"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langchain>=1.3.9",
    "langgraph>=1.2.5",
    "langsmith>=0.8.15",
]
